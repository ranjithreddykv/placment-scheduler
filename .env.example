PORT=5000
# Leave MONGODB_URI empty to auto-start an in-memory MongoDB instance for local/demo use.
# Set it to a real connection string (local mongod or Atlas) to use persistent storage.
MONGODB_URI=
CLIENT_URL=http://localhost:5173
